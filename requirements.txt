# ============================================================================
# Financial Dashboard — Python Dependencies
# ============================================================================
# Pinned versions match parent venv at:
#   C:\Users\tengiz\OneDrive\Desktop\AI აგენტი\venv\Scripts\python.exe
# Update via: pip install --upgrade <pkg> && pip freeze | grep <pkg>
# ============================================================================

# generate_dashboard_data.py და დამხმარე სკრიპტები
pandas==3.0.2
openpyxl==3.1.5
xlrd==2.0.2

# server.py (FastAPI backend)
fastapi==0.135.3
uvicorn==0.44.0
slowapi==0.1.9
apscheduler==3.11.2

# ----------------------------------------------------------------------------
# AI Advisor Phase 1 (MVP Chat) — active
# ----------------------------------------------------------------------------
anthropic==0.96.0
python-dotenv==1.2.2

# ----------------------------------------------------------------------------
# AI Advisor Phase 0B Sprint 2 (Forecasting) — active
# ----------------------------------------------------------------------------
# Both imports are LAZY inside dashboard_pipeline/ai/forecasting.py. If either
# wheel fails to install on this platform the rest of the pipeline keeps
# working; forecast_revenue will just return a clear Georgian error until the
# deps are present.
prophet>=1.1
statsmodels>=0.14

# ----------------------------------------------------------------------------
# AI Advisor Phase 0B Sprint 3 (Semantic Memory + RAG) — active
# ----------------------------------------------------------------------------
# Both imports are LAZY inside dashboard_pipeline/ai/memory.py. If either
# wheel fails to install on this platform the rest of the pipeline keeps
# working; recall_context / save_memory will just return a clear Georgian
# error with a `pip install` hint until the deps are present.
# Note: sentence-transformers pulls in torch (~200 MB) and downloads a ~120 MB
# multilingual MiniLM model on first use, cached in the HF cache directory.
chromadb>=0.5
sentence-transformers>=3.0

# ----------------------------------------------------------------------------
# AI Advisor Phase 2+ dependencies (not yet active, install when needed)
# ----------------------------------------------------------------------------
# langgraph>=0.2.0
# langchain-anthropic>=0.2.0
# python-telegram-bot>=21.0
# pydantic>=2.0.0
# httpx>=0.27.0
