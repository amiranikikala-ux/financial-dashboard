# CONTEXT HANDOFF — short brief

> **განახლდა**: 2026-04-27 morning session (**proof gate closure + KPI scope clarifier + cancelled-status fix**). Three commits: (1) `a6d2beb` — Financial + General logical proof gates landed in AGENTS.md + CLAUDE.md, first application `HANDOFF_ARCHIVE/PREVIEWS/ELIZI_252K_PROOF_2026-04-26.md`. ELIZI 252K ₾ gap fully closed: source 4,428 rows / 833,560.38 ₾ → composite-key dedup (Sprint 9 `ae91710`) → 3,211 rows / 559,372.38 ₾ = data.json EXACTLY (₾0.00). Top-12 portfolio-wide: **12/12 reconcile at ₾0.00**. (2) `9f73254` — KPI scope clarifier UI: „შემოვიდა (სულ)" / „...(ანალიზდა)" + 📐 banner. (3) **cancelled-status fix landed**: pipeline now skips `სტატუსი == 'გაუქმებული'` rows BEFORE composite-key dedup; new `cancelled_rows_dropped` + `cancelled_amount_dropped_ge` keys in `bundle.overall` mirror existing dedup tally. Portfolio-wide impact: 5,310,478 → **5,200,734 ₾** (−109,745 ₾ net inflation removed); raw cancelled drops 4,568 rows / 146,814 ₾ face-value (some were duplicates among themselves). ELIZI 559,372 → **541,562 ₾** (−17,811 ₾); coverage 34.7% → 35.5%; matched-side margin numbers unchanged (cancelled was never matched). 5 new pytest tests in `test_imported_products_cancelled_filter.py`, all PASS. Browser-verified live. Sprint A/B **PROOFED + cleaned**.
> **სტატუსი**: Phase 4A/4B/4C.2/4C.3 CLOSED · Phase 2.1/2.2/2.3/2.4/2.5/2.6/2.8/2.9 + 3.8 COMPLETE · Tier 1 + Tier 2 Sprint 1/2/3a/3b/3c/3d/3e/3f COMPLETE · 🧾 Sprint 5.1-5.6, 5.8-5.11 COMPLETE · Sprint 5.7 DROPPED · 5.12 evidence-only · Phase 4C.1 VAT-tools-scoped · Suppliers UX/DI arc 2026-04-26 · **Supplier Profitability Sprint A + B + name-in-PROTECTED rule + KPI scope clarifier + cancelled-status fix 2026-04-26/27 — CODE COMPLETE + PROOFED end-to-end + cancelled inflation removed**.

---

## რომელი დოკუმენტი რისთვისაა

| ფაილი | გამოყენება |
|---|---|
| **`CONTEXT_HANDOFF.md`** (ეს) | ახალი ჩატის startup — verified facts + do-not-touch + next step |
| **`PHASE_STATUS_MATRIX.md`** | ერთი ცხრილი ყველა phase-ის სტატუსით (live) |
| **`AGENTS.md`** | session-start checklist + GitNexus rules + Prompt Hygiene + Session Pacing + Correction Escalation |
| **`HANDOFF.md`** | evidence pointer (commit SHA → archive) |
| **`README.md`** | GitHub entry |
| `HANDOFF_ARCHIVE/AI_GENIUS_PARTNER_PLAN_v2.1_superseded_2026-04-25.md` | historical roadmap (archived 2026-04-25 — predates Phase 5 / 3.8 / Tier 2) |

**ახალი ჩატის read order**: ეს ფაილი → `AGENTS.md` → (phase-specific drill) `PHASE_STATUS_MATRIX.md`.

---

## Canonical paths & services

- **Workspace root**: `C:\Users\tengiz\OneDrive\Desktop\AI აგენტი`
- **Project**: `C:\Users\tengiz\OneDrive\Desktop\AI აგენტი\financial-dashboard`
- **Python interpreter** (canonical, parent venv): `C:\Users\tengiz\OneDrive\Desktop\AI აგენტი\venv\Scripts\python.exe`
- **Backend**: Windows Service **`FinancialDashboardBackend`** (NSSM, auto-start + auto-restart 2s, `AI_ENABLE_THINKING=true` + `PYTHONUTF8=1`, logs rotate 10MB/24h in `logs/backend_{stdout,stderr}.log`)
- **Service control**: `services.msc` · `Restart-Service FinancialDashboardBackend` (admin/UAC) · `C:\tools\nssm\nssm.exe {start|stop|restart|status|edit}`
- **⚠️ Service-restart-for-new-code**: service loads prompt at module-import time. After prompt changes, `Restart-Service` picks them up. For in-process AI tests, use `_scratch_dogfood_*.py` pattern (no service needed).
- **Tool surface (28)**: `read_data_json`(1), `compute_waybill_total`(2), `compute`(3), `forecast_revenue`(4), `recall_context`(5), `save_memory`(6), `journal_{add,list,update}_entry`(7-9), `analyze_dead_stock`(10), `prepare_supplier_brief`(11), `compute_cash_runway`(12), `compute_cash_flow_projection`(13), `build_debt_repayment_plan`(14), `simulate_scenario`(15), `mix_analyzer`(16), **`margin_radar`(17)**, `analyze_product_profitability`(18), `find_promotion_candidates`(19), `detect_trends`(20), `get_vat_reconciliation_month`(21), `explain_unaccounted_cash`(22), `record_cash_outflow`(23), `read_source_code`(24), `grep_code`(25), `read_excel_source`(26), `validate_vs_source`(27), `propose_feature`(28).

---

## Verified facts

| მეტრიკა | მნიშვნელობა |
|---|---|
| **pytest** | **2,229/2,232 green** (+5 from cancelled-status filter regression suite; 3 pre-existing failures = phase4b3 doc tests, unchanged baseline) |
| **`SYSTEM_PROMPT_KA`** | 1,163 lines |
| **Tool surface** | 28 |
| **Dashboard tabs** | 15 (incl. Store Compare, 💀 Dead Stock, ⚠️ Supplier Concentration, 📋 Debt Plan, 🧾 VAT); navigation regrouped 2→4 thematic (ყოველდღიური · გაყიდვები · ფინანსები · სტრატეგია) |
| **Pipeline cache** | 207 MB (was 864 MB — Sprint 3a slim-down) |
| **`data.json`** | **133.9 MB**, 26 sections, 21,240 waybills · **suppliers row 258** · imported_products dedup −44,454 rows / −2,090,645 ₾ false inflation removed · cancelled-status filter (`გაუქმებული`) drops further 4,568 rows / 146,814 ₾ face-value (net portfolio reduction 109,745 ₾) · every imported_products supplier carries `object_breakdown` (per-store cost) + `profitability` payload (status/totals/coverage/per_store/top_margin/bottom_margin/dead_stock/ambiguous_preview/unmatched_preview) · `unmatched_preview[].name_candidate` + `coverage.unmatched_with_candidate_*` + `profitability_summary.portfolio.unmatched_with_candidate_*` (alias workflow surface). Pipeline coverage 65.56% → **69.7%** via 3 deterministic rules (x-suffix, name candidate, name-in-PROTECTED) + cancelled-filter denominator cleanup. |
| **`data.db` (SQLite mirror)** | `rs-dashboard/public/data.db` regenerated by pipeline (`dashboard_pipeline/export_sqlite.py`). Tables: suppliers (258), supplier_aging (193), monthly_pnl (44), retail_sales_category (645), imported_suppliers (267), imported_products_top (13,344), iban_taxid_conflicts (1), meta_kv (53). **Use SQL via `sqlite` MCP for ad-hoc data checks** — much faster than Python subprocess. |
| **MCP servers (`.mcp.json`)** | gitnexus · playwright · filesystem (workspace + project) · github · **sqlite** (uvx mcp-server-sqlite → data.db) · **sequential-thinking** (deeper reasoning) · **memory** (knowledge graph at `.claude/mcp-memory.json`) · **brave-search** (web/local/news/video/image/summarizer — needs `BRAVE_API_KEY` env from https://api-dashboard.search.brave.com/, free tier 2K queries/month) · **time** (Asia/Tbilisi timezone-aware datetime) · **fetch** (URL → markdown for live page reads) · **context7** (Upstash, live library docs — reduces lib-API hallucination) |
| **Suppliers concentration** | HHI=558 (🟡 ზომიერი) · Top-5=42.2% · Top-10=54.2% · #1 candidate ჯიდიაი 17.36% · #2 კოკა-კოლა გურია 7.77% (best savings ~27,091 ₾) — wired in pipeline 0fd1040 |
| **IBAN audit** | 1 conflict found — `GE80TB1560000006156111` shared by 3 physical-person IDs (33001022152 თეკლე ხუჭუა / 33001079314 ანნა ბაღდასაროვი / 62005030272 ამირან კიკალიშვილი); surfaced in `meta.iban_taxid_conflicts` for manual review |
| **VAT cumulative gap** | **+90K ₾ net / +107K ₾ gross** (pipeline independently, Sprint 5.11 unit-fix landed). Audit's own figure: **742K ₾** (reproduces exactly from audit Excel). Pipeline-vs-audit delta = ~652K in pipeline coverage gaps (missing MAX/BOG/TBC for 11 months). Per-month match ≈0.3% where data is complete. |

---

## Commit history — recent (2026-04-23 → 2026-04-26)

| commit | sprint | summary |
|---|---|---|
| `_pending_` | **Cancelled-status filter — pipeline cleanup** | RS waybills marked `გაუქმებული` represent operations that were created in RS.ge then cancelled before completion (no goods delivered, no payment owed). Pre-fix pipeline counted them in `cost_imported_ge` / supplier totals → ~110K ₾ portfolio inflation. Fix: `dashboard_pipeline/imported_products.py` adds module constant `CANCELLED_STATUS_KA = "გაუქმებული"` and filters status-matching rows BEFORE composite-key dedup (cancelled rows would otherwise poison `seen_row_keys` and risk dropping legitimate active rows sharing the same key). New keys `cancelled_rows_dropped` + `cancelled_amount_dropped_ge` in `bundle["overall"]` mirror the existing dedup tally for visibility. Pipeline regen result: portfolio `total_amount_ge` 5,310,478 → **5,200,734 ₾** (−109,745 ₾ net real inflation removed, matches pre-fix `by_status["გაუქმებული"].total_ge` exactly); raw cancelled drops 4,568 rows / 146,814 ₾ face-value (the +37K delta vs. net is cancelled-rows-that-were-also-duplicates which previously got counted in `duplicate_rows_dropped`); coverage 65.6% → **69.7%**; data.json 140 MB → 133.9 MB. ELIZI specifically: 559,372 → 541,562 ₾ (−17,811); coverage 34.7% → 35.5%; matched-side margin numbers unchanged (cancelled was never matched to retail). 5 new pytest tests in `tests/test_imported_products_cancelled_filter.py` pin: cancelled excluded from totals/by_status/suppliers, dedup-poisoning prevention, zero-cancelled tally, raw-amount tally semantics. Full suite: **2,229 PASS / 3 FAIL** (3 pre-existing phase4b3 doc tests unchanged). Browser-verified live ELIZI ჯამი (541K/126K/6.3K/5.0% + 35.5% banner) — KPIs match data.json, console clean. d=3 audit scripts (`_verify_all_audit.py`, `verify_supplier_reconciliation.py`, `verify_reconciliation.py`, `audit_bog_rs_id_alignment.py`, `audit_all.py`) will see ~110K ₾ smaller portfolio totals — read-only consumers, may need re-baselining if hardcoded amounts. Proof artifact appended in `HANDOFF_ARCHIVE/PREVIEWS/ELIZI_252K_PROOF_2026-04-26.md`. |
| `9f73254` | **KPI scope clarifier UI** | „შემოვიდა (სულ)" / „... (ანალიზდა)" labels + 📐 banner above grid: „ანალიზი ეფუძნება X პროდუქცია Y-დან (Z% შემოდინების ღირებულებიდან). „შემოვიდა" = ყველა შემოდინება. „გავიდა / მოგება / მარჟა" = მხოლოდ ანალიზდილი ნაწილი." Pulls products_matched / products_imported / coverage.cost_pct from existing profitability payload — no pipeline change. Browser-verified live ELIZI ჯამი (559K/126K/6.3K/5.0%) + ოზურგეთი (508K/62K/-1.3K/-2.1%) — KPIs match data.json, scope banner reads consistently, console clean (only React DevTools INFO + benign meta-tag deprecation WARNING). Fixes user-flagged self-contradicting KPI from previous session. |
| `a6d2beb` | **Proof gate landed + ELIZI 252K closure** | Two new project rules in AGENTS.md + CLAUDE.md: (1) Financial/source-data proof gate — no "completed" claim on Excel/CSV-derived numbers without source→calc→output evidence (row counts, sums, formula, diff, 5+ representative spot-checks); (2) General logical proof gate — no "completed" claim without goal→implementation→verification proof (intent matched, no internal contradiction, regression risk checked). First application: `HANDOFF_ARCHIVE/PREVIEWS/ELIZI_252K_PROOF_2026-04-26.md` — ELIZI source 4,428 rows / 833,560.38 ₾ → composite-key dedup (Sprint 9, `ae91710`) → 3,211 rows / 559,372.38 ₾ = data.json (₾0.00 diff). 5 spot-checks: all are MAX code 2165 (ვინსტონ კომპაქტ ექსელ ბლუ) appearing in 2025.csv + 2026-01-02.csv overlap. Top-12 portfolio-wide check: 12/12 reconcile to data.json at ₾0.00. User's "RS.ge active 4,272/811,158" = active+completed (excl. cancelled); user's −252K/−1,061 gap = −274,188 dedup + 22,403 cancelled-inclusion. Sprint A/B PROOFED. **Subtle finding (informational)**: pipeline includes cancelled rows in cost_imported (22,403 ₾ ELIZI overstatement) — flagged as separate user-decision item, NOT auto-fixed. |
| `_pending_` | **Supplier Profitability Sprint B — UI + UNVERIFIED workflow** | **3 commits 2026-04-26 (post-handoff)**: (1) **pipeline x-suffix + name candidate** — `_match_product` ცდის `code + "x"` ბოლო deterministic shot-ად (MAX deprecated marker convention); +1 ცოცხალი match portfolio-wide (64K ₾, კოვერეიჯი 65.6% → 66.8%); ცოცხალი ელიზი 0% → 100% protected (ერთი ქემელი 1913 64K ₾ რესკიუ + 100% სიგარეტი → flip to PROTECTED). `_build_retail_name_index` + `_name_candidate_for` ჰინთს ერთვის ყოველ unmatched/ambiguous რიგზე (alias workflow surface; pipeline auto-merge-ს არ აკეთებს). 8 ახალი pytest (x-suffix uniq/multi/skip + name candidate single/multi/normalize/ambiguous-attached + portfolio aggregate). (2) **frontend Sprint B section + UNVERIFIED workflow + Hook Rules fix** — `SupplierModal.jsx` (-/+~440 line) + components.css (+~250 line) + responsive.css. ახალი „📊 პროდუქციული მოგება" სექცია 4 status-ით: verified/partial → 2x2 KPI grid + per-store toggle (ჯამი / ოზურგეთი / დვაბზუ / გაუნაწილებელი) + top-3/bottom-3/dead-stock სიები margin colors-ით; protected → იგივე მაგრამ bottom-margin HIDDEN + 8× 🔒 inline tag; unverified → 3-bucket grid (ალიასით გადარჩება / MAX-ში არ არის / სულ შემოვიდა) + ცოცხალი 10 candidate ცხრილი 💡 inline arrows-ით. ProfitProductRow + UnmatchedProductRow helper components. Pre-existing eslint Hook Rules violation (`portfolioTotal` useMemo after early return) გასწორდა move-it-up-ით. Browser smoke 4 status × ცოცხალ data ↔ რიცხვები ემთხვევა ₾0.01-მდე. (3) **docs handoff** — ეს ფაილი + PHASE_STATUS_MATRIX.md. 2,220 pytest. |
| `1018900` | **Supplier Profitability Sprint A — per-store** | **Per-store breakdown via destination tracking — pipeline complete.** `imported_products.py`: new `_build_destination_lookup` + `_resolve_destination_object` helpers (longest-variant-wins to prevent „გაღმა დვაბზუ" mis-assignment to ოზურგეთი — critical bug-prevention test in `test_supplier_profitability.py::test_destination_resolver_long_variant_wins`); row loop captures destination + normalizes via `object_mapping.json:rs_location_to_object`; supplier output gains `object_breakdown[]` (sorted by cost desc). `supplier_profitability.py`: outer-joins supplier-level imported per-store × matched products' retail per-store object_breakdown → `per_store_breakdown[]` with cost_imp/qty_imp/rev_sold/cost_sold/profit/margin per store. Margin uses ONLY matched-side numbers so it stays meaningful at partial coverage. `generate_dashboard_data.py`: passes `object_mapping=` into `collect_imported_products_bundle`. 5 new tests (per-store join, cost-only stores, sort order, empty-everywhere, destination resolver). Live: ჯიდიაი ოზურგეთი 410K imp / 493K rev / 7.9% · დვაბზუ 331K / 371K / 7.0% · გაუნაწილებელი 165K / 0 / —; კოკა-კოლა გურია ოზურგეთი 368K imp / 230K rev / 14.9% · დვაბზუ 34K / 192K / 12.8% (cross-store transfer surfaces as auditing hint); ვასაძის პური ოზურგეთი 79K imp / 5.6K rev (auditing target — bread products mostly don't barcode-match). Totals unchanged (5.31M ₾ imp, 65.6% coverage, 12.66% margin). 2,211 pytest pass (+5). |
| `8455486` | **Sprint A wiring** | Wired `build_supplier_profitability` into `generate_dashboard_data.run()` — slot after `build_supplier_concentration`, before `_write_outputs`. Loads + validates `Financial_Analysis/product_aliases.json` via `_validate_aliases.load_and_validate`, runs `build_supplier_profitability(data, aliases=safe_aliases)`, writes per-supplier `profitability` back onto each `data["imported_products"]["suppliers"][i]` (no extra fetch from frontend) and stores portfolio summary at `data["imported_products"]["profitability_summary"]`. Try/except guard mirrors supplier_concentration. Log line: `პროდუქციული მოგება: 67 verified, 77 partial, 110 unverified, 13 protected, 0 empty · ფინანსური დაფარვა 65.6%`. |
| `97e7330` | **Sprint A alias seed** | Empty `Financial_Analysis/product_aliases.json` seed with inline schema doc. Entries get added one-at-a-time after user confirms each mapping (Sprint B admin page will provide UI). |
| `3a80cd1` | **Supplier Profitability Sprint A — module** | **`dashboard_pipeline/supplier_profitability.py`** (~480 line) strict barcode JOIN. Match precedence: barcode (1-row hit) → product_code (1-row hit) → user-vetted alias → ambiguous (2+ retail rows share code, EXCLUDED from totals) → none. Per-supplier output: status (verified ≥80% cov / partial / unverified <5% cov / protected ≥80% matched cost in PROTECTED categories / empty), totals (cost_imported/cost_matched/cost_ambiguous/revenue_sold/cost_sold/profit/margin), coverage (cost_pct/product_pct/ambiguous_cost_pct/protected_cost_share_pct), top-3 / bottom-3 margin (revenue ≥100 ₾ noise floor; bottom excludes PROTECTED), dead-stock list (>120d since last sale OR rev=0), ambiguous + unmatched preview (top-5 by cost — alias candidates). PROTECTED substrings: cigarettes + alcohol (broader than mix_analyzer's cigarette-only set, kept local in module — `SUPPLIER_PROFITABILITY_PROTECTED_SUBSTRINGS`). Companion `_validate_aliases.py` enforces `confirmed_by="user"`, target exists in retail, no duplicate imported_code. 44 new tests (33 module + 11 validator). All hand-verified row-by-row on top-12 supplier from real data.json. |
| `b57ed2d` | **Sprint A preview** | `HANDOFF_ARCHIVE/PREVIEWS/SUPPLIER_PROFITABILITY_STRICT_PREVIEW.md` — full evidence-only design with live coverage numbers (75% cost-weighted, top-12 supplier 100% cov, ფუდმარტი/ელიზი 0%), Modal mocks (verified / PROTECTED / unverified renders), strict perfect-or-silent rationale. |
| `ca8e680` | **AGENTS.md scope rules** | Two new bullets: „Surgical edits — every changed line traces to task; no silent style/format/refactor side-quests" + „Task-external bug — flag to user, ask before fixing in same commit". Formalizes existing memory rules at session-start instead of relying on memory load. |
| `ae91710` | **Suppliers UX/DI 9** | **imported_products dedup across overlapping CSV files.** Root cause traced from user-flagged ტკბილი ქვეყანა modal: RS=18,511 vs cross-check=23,210 (+4,699 false). `Financial_Analysis/შემოტანილი პროდუქცია/2025.csv` and `2026-01-02.csv` overlap on year-end records → pipeline summed every duplicate row twice. Fix in `dashboard_pipeline/imported_products.py`: composite key dedupe `(waybill_ref, product_code, product_name, round(amount,2), round(quantity,4))` at row-iteration loop; tally exposed via `bundle.overall.duplicate_rows_dropped` + `duplicate_amount_dropped_ge`. Pipeline-wide impact: **45,121 dup rows removed, 2,127,714.65 ₾ false inflation eliminated** (≈28% of source rows were duplicates). ტკბილი ქვეყანა now matches RS nominal exactly (18,781). 2,162 pytest green. |
| `0fd1040` | **Suppliers UX/DI 8** | **`build_supplier_concentration` wired up in pipeline.** Function existed in `analytics_builders.py:2007` but was never called → widget always showed "data not available". Added to `run()` after `_build_analytics`, before `_write_outputs`. Live: HHI=558 (ზომიერი), Top-5=42.2%, Top-10=54.2%, Top-1 candidate ჯიდიაი 17.36%, Top-2 კოკა-კოლა გურია with best savings opportunity (~27,091 ₾). Try/except guards against AI tool path failures. |
| `e30d0bb` | **Suppliers UX/DI 7** | **IBAN/tax_id conflict detector.** New helper `_audit_iban_taxid_conflicts(script_dir)` walks BOG (header row 8) + TBC bank statements, extracts (recipient IBAN, tax_id, name) triples, builds map IBAN→{tax_ids}, flags any IBAN with >1 distinct tax_id. Excludes OWN_TAX_ID. Logger warning per conflict + summary count + `data.json:meta.iban_taxid_conflicts[]` for AI/UI. Per user rule "different ID = different company" — does NOT auto-merge, only flags for manual review (rename / data error). Found 1: `GE80TB1560000006156111` ↔ 3 physical-person IDs. |
| `d7a45a7` | **Suppliers UX/DI 6** | **Same-tax-id duplicate collapse in `_read_and_parse_rs`.** RS export inconsistently writes "(ID) NAME" vs "(ID-დღგ) NAME" vs Latin/Georgian/etc. variants — `groupby('ორგანიზაცია')` saw them as separate suppliers; bank reconciliation by tax_id then double-assigned same payment to both rows → fake overpayment. Fix: (1) suffix-prefix normalize `(taxid…)` → `(taxid)`, (2) longest-name-wins canonicalization per tax_id, (3) trailing whitespace/CR cleanup. Collapsed 5 dup-pairs (e.g. ვემედინა -5,091 fake overpayment → +370 real debt; 3G-Georgian Latin↔Georgian; ფუდმარტი შპს↔სს; მარუსია; დორა 888↔ტიტე 2024 — confirmed via BOG IBAN match `GE83BG0000000602521509GEL`). Suppliers count 263 → 258. |
| `983291e` | **Suppliers UX/DI 5** | **SupplierModal upgrade — wider + full Georgian + inline pay form + 📊 ანალიზი.** Width 380px → `clamp(420px, 52vw, 760px)`. Full Georgian translation (Payment split → 🧮 გადახდის ჩაშლა; Strict ბანკი → ბანკით დადასტურებული; Aging → 📅 ვადიანობა; AGING_BUCKET_KA `🟢 0–30 / 🟡 31–60 / 🟠 61–90 / 🔴 91–180 / 🔴 180+`; "(REFERENCE)" label stripped; "Strict supplier truth" jargon block deleted). Inline ხელით გადახდის ჩაწერა form (uses localPayments + persistLocalPayments props). 📊 ანალიზი section: საშუალო ზედნადები / წილი პორტფელში / გადახდის წილი (color-coded). Urgency signal (bucket-based, gated by `debtAfterLocal >= 1₾`); overpayment signal (`paid - effective >= 1₾`); browser-covers-debt confirmation. App.jsx now passes `localPayments + persistLocalPayments + allSuppliers + formatNumber`. |
| `14d110c` | **Suppliers UX/DI 4** | **Removed redundant "წყარო და დაფარვა" toggle from TrustBanner.** 117 lines deleted: `<button trust-banner-source-toggle>` + 2 sub-panels (გადახდის წყარო + მონაცემის საზღვარი) + `buildPaymentScopeBadges` + `buildTruthBoundaryBadges` + `sourceExpanded` state + `useState` import + jargon-laced explanation strings (`strict_bank_only_total ითვლის...`, `supplier_matching_registry.json არის primary truth...`). Audit-context details belong to AI chat, not daily UI. `truthBoundarySummary` prop kept for SupplierModal + WorkingCapital (unaffected). |
| `a9d4ade` | **Suppliers UX/DI 3** | **TrustBanner — count browser-recorded suppliers in card supplierCount.** When user records local payments via pay-card, manual + combined cards previously showed `327,292 ₾ + 0 მომწოდებელი` (mathematically impossible). `buildPaymentSplitCards()` now accepts `localExtraSuppliers` (count of distinct non-zero entries in `localPayments`); manual + combined cards add it to `supplierCount`. Live verified with 3 simulated browser entries: ბანკი 191 / ნაღდი 3 / სულ 194 (was 0/0/191). Math: 191+3=194, 4,510,587+327,292=4,837,879 ✓. |
| `e5eb0e4` | **Suppliers UX/DI 2** | **Filter own-company + zero-waybill placeholders from suppliers list.** User directive "მომწოდებლებში მხოლოდ კომპანიები რომლებმაც RS-ის ზედნადები გაწერეს, 0% შეცდომა". New `OWN_TAX_ID = "400333858"` constant in `dashboard_pipeline/constants.py` (single source of truth, was hardcoded only in `vat_reconciliation.py`). Filter at `_process_rs_suppliers` after sort, before dict conv: drop `supplier_id == OWN_TAX_ID` OR `waybills_count <= 0`. `extra_supplier_count` zeroed when filter activates so TrustBanner warning stays consistent. Live: -7 (1 own-company + 6 placeholders) → 263. |
| `5982929` | **Suppliers UX/DI 1** | **Suppliers page cleanup — 12 polish tasks** (1638(+) / 446(-)): brand block horizontal flex (utilities.css duplicate `.header-brand-block` deleted) + ჯ monogram inline-flex gap 0.22em; LiveClock custom Georgian short-month formatter (Intl ka-GE returns "Apr" fallback); period picker label dedupe; ნაღდი column auto-hide when 100% rows = 0; SCOPE column → 6×6 row-start indicator dot + KA tooltip via `PAYMENT_SCOPE_LABEL_KA`; ნომინ./დაბრ./რეალური → single ბრუნვა col with inline `−GEL X` return pill; "არაა RS ზედნადებში" 3 synthetic rows → CollapsibleSection below table; TrustBanner 3 → 1 card when `manual_journal_total = 0`; frontend `KA_TEXT_OVERRIDES` for backend-supplied English-laced phrases; DashboardTabs 2 → 4 thematic groups + `is-active-group` highlight; refresh button `min-width: 9.6rem` width-stable; staleness traffic-light dot (fresh<1h / stale<12h / very-stale>12h); ConcentrationWidget HHI/leverage bands → ქართული. |
| `5dfbf19` + `3942bb2` | **Phase 3.8** | **`margin_radar` time-series GM compression tracker landed.** Companion to Phase 2.3 mix_analyzer (snapshot rebalance) + Phase 2.9 detect_trends (single-period decomposition). Reads `retail_sales.by_category_by_month` (10,882 rows × 645 categories × 2024-07..2026-02), rolls N-month window (default 6, range 3..12), computes Δgm_pp = gm_last − gm_first per canonical category, weights by recent revenue (avg of last 2 in-window months), surfaces `compressing_categories[]` / `expanding_categories[]` / `protected_info[]` sorted by `compression_score = abs(min(Δpp,0)) × revenue_recent`. New file `dashboard_pipeline/ai/margin_radar.py` (~530 lines) reuses `mix_analyzer._canonicalize_protected` (now exposed via __all__) for cigarette merging, mirrors `trend_detector` suspicious filter (-5%..+90%), enforces thin-data filter (≥3 months in window AND recent revenue ≥ 1,000 ₾). New constants in `constants.py` (6 — window default/min/max, min months in window, revenue floor, expansion threshold +1pp). MARGIN_RADAR_TOOL schema in `tools.py` (~110 lines, slot 16 between mix_analyzer and analyze_product_profitability; TOOL_SCHEMAS 27 → 28; propose_feature stays at tail; cache_control invariant preserved) + dispatcher branch. **31 new pytest tests** (`tests/test_ai_margin_radar.py` — argument coercion, compression score formula, window selection, expansion threshold, suspicious filter, thin-data filter, protected canonicalization isolation, summary contract, error paths, tool registry, dispatcher routing). 9 sibling tests bumped (count assertion 27 → 28; positional indexes shifted; investigate-mode set adds `margin_radar`). **Live dog-food 3/3 PASS** on Sonnet 4.6 `think=True` (~\$0.19, 119s, 237K cache_read): (1) "რომელი კატეგორიის მარჟა კლებულობს?" → margin_radar(window=6,top_n=10) → live data surfaces sasmelebis cluster (kola −1.78pp, energy −4.62pp, mineral water −3.74pp, juice −4.28pp) + 🔒 cigarettes (−3.26pp, score 104K) informational; AI synthesizes 3 strategic conclusions; (2) "ბოლო 3 თვე" → margin_radar(window_months=3) — short-window honored, AI flags 0715 +25pp as likely SKU reclassification; (3) "20%-მდე mix-ის შეცვლით" → mix_analyzer (NOT margin_radar) — anti-trigger held. **2,166/2,166 pytest green** (+32 new). Per-store mode + frontend "📉 Margin Health" tab deferred to v2/v3 (per-month object_breakdown not in pipeline output yet). |
| `533c02f` | **Phase 2.3** | **`mix_analyzer` tool landed — reframed from rejected "industry_benchmark" to data-driven category mix analyzer.** Reads `retail_sales.by_category` (645 rows) → portfolio GM (14.96% live) → target 20% gap (+5.04pp / 218K ₾) → canonicalize protected cigarettes (3 label variants: "0804 \| სიგარეტი" 833K/9.93%, "სიგარეტი" 633K/11.31%, "ელ. სიგარეტი" 1.3K/16.83% → 1 entry 1,468,578 ₾ / 33.87% share / 10.53% blended) → DRAG (margin < portfolio_gm − 3pp AND share ≥ 1%) / LIFT (margin > +3pp AND share ≥ 0.5%) → greedy drag×best-lift shifts with 20% realism cap. New file `dashboard_pipeline/ai/mix_analyzer.py` (~550 lines) + 6 constants in `constants.py` (USER_TARGET_GROSS_MARGIN_PCT=20, PROTECTED_CATEGORY_SUBSTRINGS=("სიგარეტ",), DRAG/LIFT thresholds, MAX_SHIFT_PCT=20) + MIX_ANALYZER_TOOL schema in `tools.py` (TOOL_SCHEMAS 26 → 27, slot 15 before analyze_product_profitability; propose_feature stays at tail, cache_control invariant preserved) + dispatcher branch. All `sum()`-based (Sprint 3e convention, no fp drift). **21 new pytest tests** (cigarette canonicalization / protected-never-reduced safety / drag-lift classification / target gap / per-store / realism cap / empty/error paths / arg coercion / tool registry / dispatcher routing / helper-level). **Live dog-food 3/3 PASS** on Sonnet 4.6 `think=True` (~\$0.20, 117s total, 231K cache-read): (1) general 20% ask → mix_analyzer(target=20,top_n=5); (2) per-store → mix_analyzer(store="ოზურგეთი",top_n=7) yields ozurgeti-specific 13.47% GM; (3) per-SKU margin → analyze_product_profitability (anti-trigger held, mix_analyzer NOT called). **2,134/2,134 pytest green** (+21 new). No frontend, no external config — tool is text-only + data-driven per user decision. |
| `a1763a3` | **3f** | **TBC foodmart cashback per-file cache — completes per-file cache series.** Extends the Sprint 3b/3c/3d/3e template to `collect_tbc_foodmart_cashback` (smallest collector — TBC-only, no config file on disk, no object_mapping dependency). Adds module constants `FOODMART_CASHBACK_DEFAULT_PATTERNS` + `FOODMART_CASHBACK_LABEL_KA`, `_content_fingerprint_foodmart_cashback` (sha1 over sorted patterns + default pattern tuple — future code-level edits to the hardcoded list invalidate stale caches automatically), `_empty_foodmart_cashback_payload`, `_process_tbc_foodmart_cashback_file`, `_merge_foodmart_cashback_payloads`. Collector now accepts `use_cache`/`cache_path`; wired at `_collect_income_bundles`. Same `sum()`-based total convention as Sprint 3e — display total unchanged (313,725.75 ₾ / 33 lines). **Live-verify**: cold 6.51s → hot 0.019s (335x speedup). 7 new integration tests (`tests/test_foodmart_cashback_incremental.py`). 2,114/2,114 pytest green. **Per-file cache series complete** — all six income/expense collectors (retail_sales Sprint 2, samurneo 3b, expense_categories 3c, tax_flow 3d, POS terminal 3e, foodmart 3f) now read through incremental per-file cache. |
| `89338e4` | **3e** | **POS terminal income per-file cache + precision fix.** Extends the Sprint 3b/3c/3d template to `collect_tbc_card_income` + `collect_bog_pos_terminal_income` (both in `_collect_income_bundles`). Adds `_load_bog_pos_patterns`, `_load_tbc_card_income_config`, `_content_fingerprint_bog_pos`, `_content_fingerprint_tbc_card_income` (covers patterns/terminal_ids/label_ka/object_mapping — `object_mapping` must be in fingerprint because per-line `object` field depends on it), `_process_bog_pos_terminal_income_file`, `_process_tbc_card_income_file`, `_merge_bog_pos_payloads`, `_merge_tbc_card_income_payloads`. Both collectors now accept `use_cache`/`cache_path`; wired at `_collect_income_bundles:352-357`. **Behavior change (user-approved)**: merge-level totals now computed via Python `sum()` instead of iterative `total += x`, eliminating ~1e-8 floating-point drift (TBC 378,734.76000001247 → 378,734.76; BOG 1,481,042.880000011 → 1,481,042.88; same rows/line_count, cleaner accumulator). No hardcoded values in tests or production docs reference the old drift values, so no downstream regressions. **Live-verify on real corpus**: TBC 7.44s → 0.50s (14.9x), BOG 29.69s → 2.13s (13.9x); digests byte-identical across no-cache/cold/hot after rebaseline. 12 new integration tests (`tests/test_pos_terminal_income_incremental.py`) including Sprint 5.2 terminal-ID filter round-trip + terminal_ids change invalidation. 2,107/2,107 pytest green. Unblocks Sprint 3f (foodmart cashback). |
| `7404af6` | **3d** | **Cross-bank tax_flow per-file cache landed.** Extends the samurneo/expense_categories template to the only cross-bank collector (`collect_tax_flow` iterates BOTH BOG AND TBC xlsx corpora, tags rows with `ბანკი: "BOG"\|"TBC"`). Adds `_load_tax_flow_config`, `_content_fingerprint_tax_flow` (sha1 over sorted patterns + treasury_in_markers + `TAX_TREASURY_CLUSTER_NOTE_KA` + default_patterns), `_process_{bog,tbc}_tax_flow_file`, `_merge_tax_flow_file_payloads`. Single combined `_run_cached_per_file` call with a path-dispatching processor (calling the helper twice would wipe the first bank's entries as "stale" on the second pass — BOG/TBC paths are disjoint so a single call is correct). Collector now accepts `use_cache`/`cache_path`; wired at `_collect_income_bundles:412`. **Live-verify on real corpus: 31.13s → 0.055s cold→hot (561x speedup); digest byte-identical across baseline/cold/hot** (out=112,675.41 ₾ / 543 lines, in=46,071.98 ₾ / 12 lines, treasury_in=46,071.98 ₾ / 12 lines). 8 new integration tests (`tests/test_tax_flow_incremental.py`) including cross-bank treasury-marker round-trip. 2,095/2,095 pytest green. Unblocks Sprint 3e (POS terminal income) and 3f (foodmart cashback). |
| `0a81b86` | **3c** | **Bank expense_categories per-file cache landed.** Extends the samurneo template to `collect_tbc_expense_categories` + `collect_bog_expense_categories`. Renames `_run_cached_samurneo` → `_run_cached_per_file` (generic, shared by both sprints). Adds `_load_expense_categories_config`, `_content_fingerprint_expense_categories` (sha1 over full 630-line cfg blob + object_mapping + bank + other_id), `_process_{tbc,bog}_expense_categories_file`, `_merge_expense_file_payloads`. Both collectors now accept `use_cache`/`cache_path`; wired at `_collect_income_bundles:378,390`. **Live-verify: TBC 7.65s→0.12s (62x), BOG 18.11s→0.07s (246x); digests byte-identical** (TBC grand 3,758,916.09 ₾ / 7,572 lines / 52 categories; BOG 1,608,164.77 ₾ / 3,880 lines). 7 new integration tests (`tests/test_expense_categories_incremental.py`). 2,087/2,087 pytest green. Sprint 5.12 TBC numbers unchanged. |
| `8bd01e8` | **3b** | **Bank samurneo per-file cache landed.** Extends Sprint 2 retail_sales cache template to `collect_tbc_samurneo_flow` + `collect_bog_samurneo_flow`. Adds `_load_samurneo_patterns`, `_content_fingerprint_samurneo` (bank-scoped SHA1 over patterns + include_all), `_process_{tbc,bog}_samurneo_file`, `_merge_samurneo_file_payloads`, `_run_cached_samurneo`. Both collectors now accept `use_cache`/`cache_path`; wired `use_cache=True` at `_collect_income_bundles:409-410`. **Live-verify on real corpus: TBC 6.66s→0.03s (200x), BOG 23.22s→0.03s (748x); digests byte-identical with no-cache run (TBC exp 77,270/ret 168,010, BOG exp 120,703.95/ret 76,390).** 7 new integration tests (`tests/test_samurneo_incremental.py`) mirror retail_sales incremental pattern. 2,080/2,080 pytest green. Sprint 5.12 TBC shortage numbers unchanged. Unblocks Sprint 3c/3d/3e (tax_flow, TBC card_income + BOG POS, expense_categories, foodmart cashback). |
| `684eab8` | **5.12** | **TBC shortage diagnosis — evidence-only.** Per-row forensics on 2023-08..2024-03 + RS.ge source-of-truth + 8-bucket classification reveal TBC bank statement format changed in 2024-04: before the change, SH046092 and SH034467 terminal transactions were posted to transit-IBAN `GE69TB0000000251140006` with merchant-ID tags (`33001022152`, `33001023234`, `01301132349`) but no physical terminal ID embedded; after 2024-04 TBC started embedding terminal IDs in rollup rows. Sprint 5.2's terminal-ID filter correctly excludes merchant-ID rows (which would double-count in post-change months) but incidentally loses ~52K ₾ of real per-transaction income in the 8 pre-change months. No safe auto-fix: same row signature represents "real income" in pre-change months and "double-count aggregates" in post-change months. Audit's TBC figure (computed from RS.ge × 1/1.18) correctly captures all these transactions — pipeline's +90K independent gap verification is unaffected. See `HANDOFF_ARCHIVE/PREVIEWS/SPRINT_5_12_TBC_SHORTAGE_EVIDENCE.md`. |
| `3d41819` | **5.11** | **unit-error fix landed end-to-end** — `vat_reconciliation.py` now computes `gap_vs_declared_ge` = total_real_net − declared (NET basis, matches audit); adds `gap_gross_ge` + `total_real_net_ge` as explicit alternatives; removes hardcoded 98.5/94 claim from methodology; also fixes declared=0 placeholder case (was polluting gap sum by ~82K). Export + AI tools + frontend + tests (+5 new regression pins) + SYSTEM_PROMPT_KA all updated. `generate_dashboard_data.py` output log now exposes gross vs net. **2,073/2,073 pytest green.** data.json regenerated — UI/AI/Excel now show unit-correct numbers. |
| `f876012` | **5.10** | **evidence-only session** — diagnosed unit error in `gap_vs_declared_ge` (gross − net mismatch inflating gap by +702K); identified 98.5/94 cross-match as hardcoded string, not computed; mapped fix blast radius (6 production files + tests + prompts). **No production code changed.** See `HANDOFF_ARCHIVE/PREVIEWS/SPRINT_5_10_UNIT_ERROR_PREVIEW.md` + `_scratch_sprint5_10_*.json` evidence. |
| `6a4ee1b` | 5.9 | MAX-data-gap vs over-declaration: `insufficient_data` status for max=0 & (bank>0 \| declared>0); AI-facing warning prepended to summary_ka |
| `354ffe7` | 4C.1 scoped | VAT 3 tools schema Poka-yoke audit (by_shop docs, anti-triggers, STRICT do-not-guess) |
| `6e5118d` | 5.8 | per-shop by_shop breakdown (ოზურგეთი vs დვაბზუ); `tbc_per_shop_reliable` honesty flag; 22 new tests |
| `efcc79a` | 3a | cache slim-down 864→207 MB (cap retail_sales preview_rows per file) |
| `84faa43` | Phase 2.9 | `detect_trends` tool (MoM/YoY price×volume decomposition) |
| `5f94ded` | Phase 2.8 | Store Comparison page frontend |
| `e962857` | 5.6 | VAT AI live dog-food 3/3 PASS on Sonnet 4.6 |
| `cf39cd3` | 5.5 | **revenue = price × quantity fix** (9% drift → 612K cumulative revenue drop → 689K gap drop) |
| `46afd00` | 5.4 | Excel export for auditor/bookkeeper |
| `974f2db` | 5.3 | Dashboard 🧾 VAT & აუდიტი tab + cash classification UI |
| `5d45e9e` | 5.2 | TBC POS terminal-ID matching (5 physical terminals; transit-IBAN double-count eliminated) |
| `e866aa3` | 5.1.1 | `invoices_ge` ingest from RS.ge ა/ფ report (42 months, 491K ₾) + TBC overcount investigation |
| `3a18e45` | 5.1 | VAT reconciliation module + 3 AI tools + cash_outflow_journal.csv + SYSTEM_PROMPT_KA `🧾 VAT / აუდიტის კონსულტაცია` section |

All on `origin/main`. Older commits → `git log`.

---

## Live dog-food evidence (cumulative, real Sonnet 4.6 `think=True`)

- **Sprint 4B** (behavior rules), **4C.3** (summary_ka), **Phase 2.1/2.2/2.4/2.5/2.6** (tools), **5.6** (VAT trio): all **3/3 PASS** per phase. Total cost across phases ≈ $1.65.
- **Sprint 5.9** (2026-04-23) — **3/3 PASS** · 72.4s · ~$0.16. Pipeline regen activated 8 `insufficient_data` months (2022-10..12 + 2023-01..05). Scenarios:
  - (1) `2023-03 data-gap ask` — AI surfaced "MAX retail_sales Excel ფაილი აკლია" warning verbatim + explicitly said "დასკვნა «ბუღალტერს ხარვეზი აქვს» ამ ციფრებზე დამყარებით მცდარი იქნება" + prescribed upload + regen steps.
  - (2) `loaded over-declaration framing rejection` — AI refused to confirm premise, surfaced audit cross-check showing declared 49,379 ₾ ≈ audit 49,762 ₾, offered multi-hypothesis (50%/35%) and flagged MAX file absence as root cause.
  - (3) `2024-08 real month control` — AI correctly did NOT emit the data-gap warning (no false positive); routed to `get_vat_reconciliation_month` + `explain_unaccounted_cash` chain for the real 139K gap + 168K unaccounted cash + 30K VAT liability preview.
- **Phase 4C.1 Parts A+B routing** (2026-04-24) — **4/4 PASS** · ~109s · ~$0.25. Confirms 8-tool schema tightening (eacc59b + 3d13e8b) actually changes routing on real turns:
  - (1) `"2026-02-27-ში რამდენი ზედნადები?"` → `compute_waybill_total` (Part B Triggers block fired)
  - (2) `"რა პროდუქცია მაქვს 120+ დღე გაუყიდავი?"` → `analyze_dead_stock` ONLY (Part B anti-trigger kept analyze_product_profitability + find_promotion_candidates out — distinction between frozen vs live SKUs held)
  - (3) `"ფუდმარტს რამდენი ვალი?"` (no date) → `read_data_json(supplier_aging)` + `compute`, NOT `compute_waybill_total` (Part B anti-trigger for undated debt questions held)
  - (4) `"2024-08-ში declared vs რეალური?"` → `get_vat_reconciliation_month` (Part A anti-trigger away from read_data_json held).
- **Phase 3.8 margin_radar** (2026-04-25, post-commit `5dfbf19`) — **3/3 PASS** · 119s · ~$0.19 · 237K cache_read · Sonnet 4.6 `think=True`. Scenarios: (1) "რომელი კატეგორიის მარჟა კლებულობს?" → margin_radar(window=6, top_n=10) — sasmelebis cluster (kola −1.78pp, energy −4.62pp, mineral water −3.74pp, juice −4.28pp) + 🔒 cigarettes (−3.26pp, score 104K) informational; AI synthesized 3 strategic conclusions including "სასმელების კატეგორია — სისტემური პრობლემა"; (2) "ბოლო 3 თვის განმავლობაში" → margin_radar(window_months=3) — short window honored, AI flags 0715 +25pp expansion as likely SKU reclassification, recommends Excel verification; (3) "20%-მდე mix-ის შეცვლით" → mix_analyzer (NOT margin_radar) — anti-trigger held, snapshot vs time-series distinction respected.
- **Sprint 5.11 unit-fix** (2026-04-24, post-commit bfeeee5) — **1/1 PASS** · 27.7s · ~$0.05 · in-process Sonnet 4.6 `think=True`. Scenario: `"2024 აგვისტოში declared vs რეალური ბრუნვა — რა არის გაპი?"` → AI called `get_vat_reconciliation_month` + `explain_unaccounted_cash`, emitted explicit "NET საფუძველი (პირველადი, audit-matched)" table with declared 139,485 / total_real_net 236,169 / gap_net +96,684 / gap_gross +114,087 labeled as alternative. Verified: "ნეტო" surfaced, no trace of old 906K headline, thinking=True, no anti-markers. In-process test — confirms production AI will behave correctly once `FinancialDashboardBackend` service restarts (admin/UAC required) to pick up the new prompt + tools cache.

---

## Do-not-touch rules (carry forward)

1. **Backend interpreter = parent venv only** (`...\AI აგენტი\venv\Scripts\python.exe`). Never `.venv` / project-local / system Python.
2. **Windows venv verification** = `Get-CimInstance Win32_Process -Filter "ProcessId=X" | Select CommandLine` (NOT `Get-Process <pid> | Select Path` — stub artifact always shows base interpreter).
3. **Env propagation** = verify `usage.thinking=true` in a real SSE call after any backend restart.
4. **Investigator prompt** (`SYSTEM_PROMPT_KA_INVESTIGATOR`) stays marker-free for chat-mode sections. Every Phase 4B rule has a do-not-touch investigator guard.
5. **GitNexus index** may be stale. If any GitNexus tool warns stale, run `npx gitnexus analyze` first.
6. **ChromaDB 1.5.x `$lt`/`$gt`** don't work on string metadata — Python post-fetch filter mandatory.
7. **Excel Georgian path**: `tools.py::_resolve_safe_path` uses `Path.absolute()`, NOT `Path.resolve()`.
8. **Prompt editing**: 600+ grep-style assertions pin `SYSTEM_PROMPT_KA`. Before any edit, grep tests for asserted Georgian phrases.
9. **4B rule conflict resolution**: Phase 4B rules sit above phase-specific sections. If conflict, principle wins.
10. **`.claude/scheduled_tasks.lock`** gitignored (Claude Code ScheduleWakeup artifact, machine-specific).
11. **data.json regen**: Sprint 5.9 requires pipeline regen to activate `insufficient_data` status on months 2022-10/11/12 + 2023-01..05. If data.json is older than commit `6a4ee1b`, rerun `python generate_dashboard_data.py` before any VAT drill-down.
12. **retail_sales revenue (Sprint 5.5)**: formula is `unit_price × quantity` per row. Pinned in `tests/test_retail_sales_revenue_formula.py`. Never regress to summing unit_price alone.
13. **Supplier-product JOIN (Sprint A 2026-04-26)**: barcode/code only (1:1, exactly-one-row); `code + "x"` MAX-deprecated-marker added 2026-04-26 (`2e685dc`). Name-fuzzy auto-match FORBIDDEN as a generic rule — Borjomi glass and plastic share name but are different SKUs (memory `project_product_match_barcode_only.md`). 2+ retail-row hits = ambiguous = excluded from totals. Aliases must have `confirmed_by="user"`. **Exception added 2026-04-26 (`da03514`)**: unique normalized name in PROTECTED retail category (cigarettes / alcohol per `SUPPLIER_PROFITABILITY_PROTECTED_SUBSTRINGS`) auto-merges; beverages are NOT protected, so the Borjomi rule still holds.
14. **UI rule clarified 2026-04-26 (memory `feedback_perfect_or_silent.md`)**: 100% verified OR explicit step-by-step path to fill the gap (alias candidates, missing source). Never silent dead-end "ჯერ უცნობია". Coverage % can appear in main UI but must be paired with concrete next-step language.
15. **Destination resolver order**: `_build_destination_lookup` sorts variants by length descending so the longest-specific match wins (prevents „ოზურგეთი სოფ. გაღმა დვაბზუ" from being mis-assigned to ოზურგეთი). If you add new variants to `object_mapping.json:rs_location_to_object`, the test `test_destination_resolver_long_variant_wins` will catch a regression.
16. **🚨 Financial/source-data proof gate (NEW 2026-04-26, CLAUDE.md + AGENTS.md)**: Excel/CSV/JSON-დან ამოღებულ ფინანსურ/გაყიდვების/მარჟის/მაღაზიის/პროდუქტის ციფრზე "დასრულებულია" აკრძალულია სანამ არ არსებობს source→calculation→output proof: წყაროს row count + sum, formula, output sum, diff, **5 representative spot-checks**. Diff უნდა იყოს 0 ან source-level მიზეზით ახსნილი. Source canonical path: `C:\Users\tengiz\OneDrive\Desktop\AI აგენტი\Financial_Analysis`. თუ ვერ დამტკიცდი → დაწერე "კოდი გავაკეთე, მაგრამ ციფრები ჯერ არ დამიმტკიცებია". This rule **caught open issue #0 above** — 252K ₾ ELIZI source-vs-pipeline diff would have been silently absorbed.
17. **🚨 General logical proof gate (NEW 2026-04-26, AGENTS.md)**: ნებისმიერ task-ზე "დასრულებულია" აკრძალულია სანამ არ არსებობს goal→implementation→verification proof: user-ის მოთხოვნა სწორად არის გაგებული, ცვლილება ზუსტად ამ მოთხოვნას ემსახურება, შედეგი შიგნით არ ეწინააღმდეგება საკუთარ ლოგიკას (e.g., KPI cells with mismatched scope = self-contradicting), გვერდითი ეფექტი / regression risk შემოწმებულია, verification result მკაფიოდ წერია. ვერ დამტკიცდი → "გაკეთებულია, მაგრამ ბოლომდე დამტკიცებული არ არის". Triggered by user pushback on KPI scope-mismatch (ჯამში 559K vs ოზურგეთში 508K with -2.1% margin) — internal-logic check is now mandatory pre-claim.

---

## Still-open work (priority order, fresh-session recommended for high-risk)

| # | item | size | risk | რატომ |
|---|---|---|---|---|
| 1 | **Supplier Profitability Sprint C — alias UI mutation workflow** | ~1 session | LOW | Sprint B landed UI; UNVERIFIED status-ი ცოცხლად აჩვენებს candidate-ცხრილს „💡 MAX-ის შესაძლო შესაბამისობა". User-ის დადასტურების წერტილი ჯერ მხოლოდ ფაილში — `Financial_Analysis/product_aliases.json`-ში ხელით და ხელახლა pipeline. Sprint C: (1) ბრაუზერის "✓ დაადასტურე ალიასი" ღილაკი (POST endpoint → JSON file write); (2) optional როლბექი/გაუქმება. Validator (`_validate_aliases.py`) ისედაც მუშაობს. Portfolio-wide candidate-ცხრილი (cumulative ~16 elizi + ~247 ფუდმარტი + სხვა — ცოცხალი 394K ₾ რესკიუ პოტენციალი) admin გვერდიდან. Cigarette/ალკოჰოლი ცალკე ვიზუალური ფლაგი დაწესების გასათვალისწინებლად (PROTECTED-ზე გადასვლა). Alias-ის ცოცხალი workflow ერთი-კლიკი = ცოცხალ pipeline-ის რეგენი (აზინქრონული) ან მხოლოდ ფაილის შენახვა + manual regen. **Pre-req**: pipeline + UI ცოცხლად მუშაობს, scope მხოლოდ მუთაცია. |
| 2 | **AI tool wrapper — `analyze_supplier_profitability(tax_id)`** | ~1 session | LOW | Claude tool-ი რომელიც ცოცხლად ჰკითხავს „ფუდმარტიდან რომელი პროდუქცია არ ღირს?" → ცოცხალი status + top/bottom/dead-stock + ალიასის candidate-ცხრილი + ცალკე source-ის მოთხოვნა. ცარიელი status-ისთვის (UNVERIFIED) AI ცხადად შესთავაზებს „X პროდუქცია (Y ₾) ალიასის გაკეთება შეიძლება — მე გავაკეთო?". TOOL_SCHEMAS 28 → 29. Live dog-food 3-სცენარი Sonnet 4.6 think=True. |
| 1 | **Phase 4C.1 Part C (if gaps appear)** | evidence-driven | LOW | Parts A+B covered 8 tools. Remaining tools already carry Triggers + Anti-triggers + Returns + Honesty-rule blocks per evidence survey. Only open new schema-audit work if a live dog-food surfaces an actual routing miss. |
| 2 | **Phase 2.3 mix_analyzer — follow-ups** | ~1 session each | LOW-MED | (a) Frontend dashboard tab for mix visualization (would be 16th tab — separate UI sprint; deferred per preview §7). (b) Upstream category canonicalization in `retail_sales.py` so `analyze_product_profitability` + `detect_trends` see merged cigarette rows too (Option B of preview §4). (c) `user_targets.json` config externalization once targets settle. (d) Sensitivity probe dog-food ("რა ხდება 18%-ზე?") to verify `target_gross_margin_pct` override path under think=True. |
| 3 | **Phase 3 remaining (3 features)** | ~1 week | LOW | conversation_summary_on_demand · monthly_strategy_page · gap_analysis (margin_compression_radar landed `5dfbf19`) |
| 4 | **Phase 4 Advanced (9 features)** | ~2-3 weeks | MED | in archived v2.1 roadmap (`HANDOFF_ARCHIVE/AI_GENIUS_PARTNER_PLAN_v2.1_superseded_2026-04-25.md`) |
| 5 | **Parking Lot** | — | — | ~40 items in v2.1 plan |

**Recently CLOSED** (drill into commit/evidence if needed, not open work):
- **Supplier Profitability proof gate closure + KPI scope clarifier 2026-04-27 (`a6d2beb` + `9f73254`, 2 commits)** — proof artifact `HANDOFF_ARCHIVE/PREVIEWS/ELIZI_252K_PROOF_2026-04-26.md` closes ELIZI 252K source-vs-pipeline diff at ₾0.00; top-12 reconcile portfolio-wide; KPI scope clarifier UI fixes self-contradicting „559K imported / -2.1% margin" cell display; live browser-verified. Sprint A/B is now PROOFED end-to-end. Open: cancelled-status pipeline inflation flagged as separate user-decision.
- **Suppliers UX & Data Integrity arc 2026-04-26 (`5982929` → `ae91710`, 9 commits)** — full Suppliers page redesign + root-level data quality fixes. Page-level: 11→7 column table, modal 380→720px with inline pay form + 📊 ანალიზი + urgency/overpayment signals, TrustBanner collapses single-card on manual=0 + `localPayments` count math fixed, DashboardTabs 2→4 thematic groups, ჯ monogram brand. **Data integrity (the heavy stuff)**: own-company filter (OWN_TAX_ID const → -1 row), 0-waybill placeholder filter (-6 rows), same-tax-id collapse via canonical name (-5 dup-pairs incl. დორა 888↔ტიტე 2024 confirmed via shared BOG IBAN), IBAN audit (1 cross-ID conflict surfaced), supplier_concentration wired (was null-forever), imported_products dedup (45,121 false rows / 2,127,714 ₾ inflation removed across overlapping CSV files). All ~258-suppliers / data.json now sound for AI/Excel/UI. **No new dog-food this arc** (UI/data-quality work; existing 28-tool surface unchanged). 2,162 pytest pass.
- **Phase 3.8 (`5dfbf19` + `3942bb2`) — `margin_radar` time-series GM compression tracker landed**; reuses mix_analyzer's protected canonicalization for cigarettes per period; window default 6m (range 3..12); compression_score = abs(min(Δpp,0)) × revenue_recent; protected entries surface in `protected_info[]` separately, never in compressing_categories[]; 31 new pytest; live dog-food 3/3 PASS Sonnet 4.6 `think=True` (~\$0.19); TOOL_SCHEMAS 27 → **28**; preview `HANDOFF_ARCHIVE/PREVIEWS/SPRINT_3_8_MARGIN_RADAR_PREVIEW.md`
- **Phase 2.3 (`533c02f`) — `mix_analyzer` category mix analyzer landed**; cigarette 3-variant canonicalization; DRAG/LIFT bands + realism cap; 21 new pytest tests; live dog-food 3/3 PASS Sonnet 4.6 `think=True` (~\$0.20); TOOL_SCHEMAS 26 → 27
- Sprint 3f (`a1763a3`) — foodmart cashback per-file cache; 335x hot speedup; 7 new tests (2,114/2,114 green); **per-file cache series COMPLETE** across all 6 income/expense collectors
- Sprint 3e (`89338e4`) — POS terminal income per-file cache + precision fix; TBC 14.9x / BOG 13.9x hot speedup; 12 new tests; user-approved `sum()` total convention (removes ~1e-8 fp drift)
- Sprint 3d (`7404af6`) — cross-bank tax_flow per-file cache landed, 561x hot speedup on real corpus (31.13s → 0.055s), 8 new tests; single combined `_run_cached_per_file` call with path-dispatching processor (BOG/TBC paths disjoint)
- Sprint 3c (`0a81b86`) — expense_categories per-file cache landed, 62x TBC / 246x BOG speedup, 7 new tests; `_run_cached_samurneo` generalized to `_run_cached_per_file`
- Sprint 3b (`8bd01e8`) — samurneo per-file cache landed, 200x TBC / 748x BOG speedup, 7 new tests
- Sprint 5.12 (`684eab8`) — TBC shortage 2023-08→2024-03 diagnosed, no safe auto-fix, audit-defense unaffected (see `HANDOFF_ARCHIVE/PREVIEWS/SPRINT_5_12_TBC_SHORTAGE_EVIDENCE.md`)
- Sprint 5.11 (`bfeeee5` + `3d41819`) — VAT gap unit-error fixed end-to-end, live AI 1/1 PASS

**Audit defense view** (rewritten 2026-04-24 after Sprint 5.11 unit-fix landed, TBC line sharpened by Sprint 5.12 diagnosis):

- **Real gap (net, audit-matched): 742K ₾.** Audit's Excel "სხვაობა ბრუნვაში" = 4,645,366 (net sum) − 3,903,150 (declared net) = **742,217 ₾** — reproduces exactly from the audit file.
- **Pipeline independently verifies 90K ₾ (net) / 107K ₾ (gross)** directly. Per-month check (2024-08, 2025-08, 2025-12): pipeline_gross ≈ audit_net × 1.18 within **0.3%** when data is complete.
- **Remaining ~652K is within pipeline coverage gaps** — not a disagreement with audit:
  - missing MAX POS Excel files: 2022-10..12 + 2023-01..05 (8 months, `status=insufficient_data`)
  - missing BOG bank statements: 2023-Q1 (3 months, pipe_bog=0, audit has ~34K)
  - **TBC statement format pre-2024-04** (Sprint 5.12): 8 months (2023-08..2024-03), ~52K ₾ of SH046092/SH034467 per-transaction income posted to transit-IBAN with merchant-ID tags only (no physical terminal ID in text). Sprint 5.2's terminal-ID filter correctly excludes them to prevent double-counting in post-change months; audit's figure (RS.ge × 1/1.18) captures them independently. No safe auto-fix.
- **Old "98.5% TBC / 94% BOG match"** (hardcoded) and **"906K real gap"** (unit error) — both WITHDRAWN in Sprint 5.10 evidence + Sprint 5.11 code fix. UI/AI/Excel now reflect correct numbers.
- **For the auditor**: audit's 742K figure is correct. Pipeline agrees unit-for-unit where data is complete. Any discussion of "real gap" should cite 742K (net, audit-matched), optionally qualified with "pipeline-independent verification is +90K due to coverage gaps in months X, Y, Z — not a methodological disagreement."

---

## `მოამზადე ახალი ჩატისთვის` — rule

- განაახლე ეს ფაილი (verified facts + do-not-touch + next step only).
- **არ შეაყრო ისტორია** — git commit log = authoritative, `HANDOFF_ARCHIVE/` = preview-ები/evidence, `HANDOFF.md` = commit SHA → archive index.
- Short brief target: **≤200 lines**. ისტორიული "წინა-სტატუსი" სექციები აკრძალულია.
